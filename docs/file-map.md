# File Map

Every Python file in `backend/`, its single responsibility, what it imports from internally, and what imports it.

| File | Responsibility | Depends on (internal) | Called by |
|------|---------------|----------------------|-----------|
| `backend/__init__.py` | Package marker (empty stub) | — | Python import system |
| `backend/main.py` | FastAPI app factory, lifespan startup sequence, singleton wiring, SPAStaticFiles cache-control logic, global exception handlers | `config`, `database`, all `api/*`, all service singletons | uvicorn entry point |
| `backend/config.py` | Pydantic-settings singleton; all env var definitions and defaults; CREDENTIAL_KEY auto-generation; PROJECT_ROOT and DATA_DIR path constants | — | everything |
| `backend/database.py` | Async SQLAlchemy engine (sqlite+aiosqlite), WAL mode hook, AsyncSessionLocal factory, db_session() context manager, get_db() generator, init_db() with migrations and seed | `config`, `models` (Base, JobSource), `scraping.site_prompts` | `main`, all `api/*` routers, `scheduler.morning_batch` |
| `backend/defaults.py` | Module-level numeric constants (DAILY_LIMIT, MIN_MATCH_SCORE, CONCURRENCY_GEMINI, field length limits, embedding thresholds) | — | `applier.*`, `matching.*`, `scraping.*`, `scheduler.morning_batch`, `security.sanitizer` |
| `backend/api/__init__.py` | Package marker (empty stub) | — | Python import system |
| `backend/api/deps.py` | DBSession type alias (Annotated AsyncSession via Depends); singleton getter functions for app.state objects | `database.get_db` | all `api/*` routers |
| `backend/api/jobs.py` | GET /api/jobs, GET /api/jobs/{id}, POST /api/jobs/search, GET /api/jobs/{id}/score endpoints | `api.deps`, `models.job`, `matching.filters`, `scraping.adzuna_client`, `scraping.deduplicator` | `main` |
| `backend/api/queue.py` | GET /api/queue, GET /api/queue/status, POST /api/queue/refresh, GET /api/queue/{id}, PATCH /api/queue/{id}/skip, PATCH /api/queue/{id}/status, POST /api/queue/{id}/enrich-description | `api.deps`, `models.job`, `scraping.scrapling_fetcher` | `main` |
| `backend/api/applications.py` | GET/POST /api/applications, GET/PATCH /api/applications/{id}, POST /api/applications/{id}/events, POST /api/applications/{match_id}/apply | `api.deps`, `models.application`, `models.document`, `models.job`, `models.user`, `applier.engine` | `main` |
| `backend/api/documents.py` | GET /api/documents, POST /api/documents/validate-template, GET /api/documents/{id}/cv/pdf, GET /api/documents/{id}/letter/pdf, GET /api/documents/{id}/diff, POST /api/documents/{id}/regenerate | `api.deps`, `models.document`, `models.job`, `latex.parser` | `main` |
| `backend/api/settings.py` | Profile, search-settings, sources, credentials, sites, and custom-sites CRUD endpoints; Fernet encrypt/decrypt for credentials | `api.deps`, `config`, `models.job`, `models.user`, `scraping.site_prompts` | `main` |
| `backend/api/analytics.py` | GET /api/analytics/summary, GET /api/analytics/trends endpoints | `api.deps`, `models.application`, `models.job` | `main` |
| `backend/api/ws.py` | WebSocket /ws endpoint; ConnectionManager (asyncio.Lock, UUID-keyed connections, register_handler, broadcast, send_to); broadcast_status() and broadcast_job_assessment() helpers | — | `main`, `scraping.orchestrator`, `scheduler.morning_batch`, `applier.form_filler`, `scraping.session_manager` |
| `backend/api/ws_models.py` | Pydantic WS message schemas: ScrapingStatus, MatchingStatus, TailoringStatus, ApplyReview, ApplyResult, LoginRequired, LoginConfirmed, ErrorMessage (server→client); ConfirmSubmit, CancelApply, LoginDone, LoginCancel (client→server) | — | `api.ws`, `applier.form_filler`, `scraping.session_manager` |
| `backend/applier/__init__.py` | Package marker (empty stub) | — | Python import system |
| `backend/applier/engine.py` | ApplicationEngine singleton: daily limit guard, per-job asyncio Event concurrency guard, strategy dispatch (Auto/Assisted/Manual), Application + ApplicationEvent DB persistence, signal_confirm/signal_cancel WS bridges | `applier.auto_apply`, `applier.assisted_apply`, `applier.manual_apply`, `applier.daily_limit`, `config`, `defaults`, `models.application`, `models.job` | `api.applications`, `main` |
| `backend/applier/auto_apply.py` | AutoApplyStrategy: Tier 1 PlaywrightFormFiller → Tier 2 browser-use agent fallback; ApplyReview WS broadcast; confirm/cancel event gate | `applier.form_filler`, `config`, `llm.gemini_client` | `applier.engine` |
| `backend/applier/assisted_apply.py` | AssistedApplyStrategy: Tier 1 PlaywrightFormFiller.fill_only() → Tier 2 browser-use agent fallback; browser left open for manual submission | `applier.form_filler`, `config`, `llm.gemini_client` | `applier.engine` |
| `backend/applier/manual_apply.py` | ManualApplyStrategy: opens apply_url in system browser via webbrowser.open(); ApplicationResult stub | — | `applier.engine` |
| `backend/applier/form_filler.py` | PlaywrightFormFiller: launch_persistent_context → CAPTCHA check → _clean_form_html → _build_fill_prompt → Gemini call → fill + upload → apply_review WS → confirm/cancel gate → submit | `api.ws`, `api.ws_models`, `applier.captcha_handler`, `config`, `llm.gemini_client` | `applier.auto_apply`, `applier.assisted_apply` |
| `backend/applier/captcha_handler.py` | CAPTCHA detection and handling helpers for Playwright pages; _domain_key() for profile-dir naming | — | `applier.form_filler`, `applier.auto_apply` |
| `backend/applier/daily_limit.py` | DailyLimitGuard: counts today's applied/pending rows; assert_can_apply() raises DailyLimitExceeded | `models.application` | `applier.engine`, `scheduler.morning_batch` |
| `backend/latex/__init__.py` | Package marker (empty stub) | — | Python import system |
| `backend/latex/pipeline.py` | CVPipeline (JobAnalyzer → CVModifier → CVApplicator → Tectonic; per-job JobContext cache TTL 1h/max 100); LetterPipeline (LaTeXParser → CVEditor → LaTeXInjector → Tectonic); TailoredCV / TailoredLetter / DiffEntry dataclasses; generate_diff() legacy helper | `latex.compiler`, `latex.injector`, `latex.parser`, `llm.gemini_client`, `models.schemas` | `scheduler.morning_batch`, `api.documents`, `main` |
| `backend/latex/compiler.py` | LaTeXCompiler: async Tectonic subprocess wrapper; _find_tectonic() walks PATH then project bin/; LaTeXCompilationError | — (stdlib only) | `latex.pipeline`, `latex.validator` |
| `backend/latex/applicator.py` | CVApplicator safety gate: confidence ≥ 0.7 + verbatim substring check + no-new-LaTeX-commands check; cap MAX_REPLACEMENTS=3; _has_new_latex_commands() helper | `llm.validators` | `latex.pipeline` |
| `backend/latex/injector.py` | LaTeXInjector: inject_summary_edit(), inject_experience_edits(), inject_letter_edit() — splice LLM edits into JOBPILOT marker-delimited sections | — (stdlib only) | `latex.pipeline` |
| `backend/latex/parser.py` | LaTeXParser: MARKER_RE regex extracts SUMMARY/EXPERIENCE/LETTER:PARA sections; validate_markers() consistency check; extract_bullets(); LaTeXSections dataclass | — (stdlib + optional texsoup) | `latex.pipeline`, `api.documents` |
| `backend/latex/validator.py` | LaTeXValidator: validate a .tex string (marker presence, compile test); used by API endpoint | `latex.compiler`, `latex.parser` | `api.documents` |
| `backend/llm/__init__.py` | Package marker (empty stub) | — | Python import system |
| `backend/llm/gemini_client.py` | GeminiClient: 15 RPM sliding-window deque rate limiter, primary+fallback model chain, 429 exponential back-off, generate_text(), generate_json() with native JSON mode + self-healing retry, embed(); GeminiRateLimitError, GeminiJSONError | `config`, `defaults` | `llm.job_analyzer`, `llm.cv_modifier`, `llm.cv_editor`, `applier.auto_apply`, `applier.assisted_apply`, `applier.form_filler`, `main` |
| `backend/llm/job_analyzer.py` | JobAnalyzer: single generate_json() call → JobContext; sanitizes inputs; strips LaTeX preamble via _strip_preamble | `llm.gemini_client`, `llm.cv_modifier` (_strip_preamble), `llm.job_context`, `llm.prompts`, `models.schemas`, `security.sanitizer` | `latex.pipeline` |
| `backend/llm/cv_modifier.py` | CVModifier: modify() (whole CV + JobContext → at most 3 CVReplacement) and modify_from_assessment() (FitAssessment gap-driven path); _strip_preamble() exported helper | `llm.gemini_client`, `llm.job_context`, `llm.prompts`, `llm.validators`, `models.schemas`, `security.sanitizer` | `latex.pipeline` |
| `backend/llm/cv_editor.py` | CVEditor: generate_json() on MOTIVATION_LETTER_PROMPT for LETTER:PARA section; post-edit no-new-LaTeX-commands safety check | `latex.parser`, `llm.gemini_client`, `llm.prompts`, `llm.validators`, `models.schemas`, `security.sanitizer` | `latex.pipeline` |
| `backend/llm/job_context.py` | JobContext Pydantic model (required_skills, gaps, keywords, hints, do_not_touch); to_markdown() serializer | — | `llm.job_analyzer` (produces), `llm.cv_modifier` (consumes) |
| `backend/llm/prompts.py` | Prompt string constants: MOTIVATION_LETTER_PROMPT, JOB_ANALYZER_PROMPT, CV_MODIFIER_SKILL, CV_MODIFIER_FROM_ASSESSMENT | — | `llm.job_analyzer`, `llm.cv_modifier`, `llm.cv_editor` |
| `backend/llm/validators.py` | Pydantic output validators: CVReplacement (with is_applicable()), CVModifierOutput (with top_three()), LetterEdit | — | `llm.cv_modifier`, `llm.cv_editor`, `latex.applicator` |
| `backend/matching/__init__.py` | Package marker (empty stub) | — | Python import system |
| `backend/matching/matcher.py` | JobMatcher: score() (keyword 40% + location 20% + experience 15% + salary 10% + recency 10%); rank_and_filter() batch scorer | `defaults`, `matching.filters`, `models.schemas` | `scheduler.morning_batch`, `main` |
| `backend/matching/filters.py` | JobFilters dataclass: matching criteria fields derived from SearchSettings | `defaults` | `scheduler.morning_batch`, `matching.matcher`, `scraping.orchestrator`, `scraping.adzuna_client`, `api.jobs` |
| `backend/matching/fit_engine.py` | FitEngine.assess(): cosine-similarity skill-gap assessment; FitAssessment, SkillGap dataclasses; should_modify threshold by sensitivity mode | `defaults`, `matching.cv_parser`, `matching.job_skill_extractor` | `scheduler.morning_batch` |
| `backend/matching/cv_parser.py` | CVParser: parses LaTeX CV sections (Profile/Skills/Experience) with context-weighted SkillEntry list; build_profile() returns CVProfile with SHA-256 hash | `matching.skill_patterns` | `scheduler.morning_batch`, `matching.embedder`, `matching.fit_engine` |
| `backend/matching/embedder.py` | Embedder: wraps GeminiClient.embed() to populate SkillEntry.embedding on CVProfile and JobProfile objects; batch-safe async | `llm.gemini_client`, `matching.cv_parser`, `matching.job_skill_extractor` | `scheduler.morning_batch` |
| `backend/matching/job_skill_extractor.py` | JobSkillExtractor.extract(): parses job description text into a JobProfile with criticality-weighted JobSkill objects | `matching.skill_patterns` | `scheduler.morning_batch`, `matching.fit_engine` |
| `backend/matching/skill_patterns.py` | TECH_PATTERN compiled regex for recognising tech skill tokens in free text | — | `matching.cv_parser`, `matching.job_skill_extractor` |
| `backend/models/__init__.py` | Re-exports Base and all ORM models so `from backend.models import Base` works | `models.base`, `models.job`, `models.application`, `models.document`, `models.user`, `models.session` | `database.init_db` |
| `backend/models/base.py` | SQLAlchemy DeclarativeBase definition (`Base`) | — | all `models.*` ORM classes |
| `backend/models/job.py` | ORM models: JobSource (job_sources table), Job (jobs table, dedup_hash unique), JobMatch (job_matches table, with ats_score/gap_severity/fit_assessment_json) | `models.base` | `api.jobs`, `api.queue`, `api.applications`, `api.settings`, `scheduler.morning_batch`, `matching.matcher`, `applier.engine` |
| `backend/models/application.py` | ORM models: Application (applications table), ApplicationEvent (application_events table) | `models.base` | `api.applications`, `applier.engine`, `applier.daily_limit`, `api.analytics` |
| `backend/models/document.py` | ORM model: TailoredDocument (tailored_documents table; tex_path, pdf_path, diff_json, llm_prompt, llm_response) | `models.base` | `api.documents`, `api.applications`, `scheduler.morning_batch` |
| `backend/models/user.py` | ORM models: UserProfile (user_profile singleton), SearchSettings (search_settings singleton), SiteCredential (site_credentials, Fernet-encrypted) | `models.base` | `api.settings`, `api.applications`, `scheduler.morning_batch`, `scraping.session_manager` |
| `backend/models/session.py` | ORM model: BrowserSession (browser_sessions table; storage_state_path, expires_at) | `models.base` | (seeded/queried by applier pipeline) |
| `backend/models/schemas.py` | Pydantic DTOs: RawJob (scraper output, field-length-capped), JobDetails (matcher/pipeline input, optional id/score) | — | `scraping.*`, `matching.*`, `latex.pipeline`, `scheduler.morning_batch` |
| `backend/scheduler/__init__.py` | Package marker (empty stub) | — | Python import system |
| `backend/scheduler/morning_batch.py` | MorningBatchRunner: on-demand 6-step pipeline (load settings → scrape → rank → store → fit-assess → CV pre-generation with Semaphore(3) → WS broadcast); APScheduler import present but never started | `config`, `defaults`, `applier.daily_limit`, `matching.cv_parser`, `matching.embedder`, `matching.filters`, `matching.fit_engine`, `matching.job_skill_extractor`, `models.*`, `scraping.orchestrator` (via self._scraper), `latex.pipeline` (via self._cv_pipeline), `api.ws` | `api.queue`, `main` |
| `backend/scraping/__init__.py` | Package marker (empty stub) | — | Python import system |
| `backend/scraping/orchestrator.py` | ScrapingOrchestrator: three-phase pipeline (Phase 1 Adzuna parallel API; Phase 2 browser sites sequential Tier1→Tier2; Phase 3 lab URLs parallel); post-scrape date filter; dedup | `models.schemas`, `scraping.site_prompts`, `scraping.adaptive_scraper`, `scraping.adzuna_client`, `scraping.deduplicator`, `scraping.scrapling_fetcher`, `scraping.session_manager`, `matching.filters`, `api.ws` | `scheduler.morning_batch`, `main` |
| `backend/scraping/scrapling_fetcher.py` | ScraplingFetcher (Tier 1): build search URL → StealthyFetcher/Fetcher HTTP → _clean_html (lxml+markdownify) → single Gemini extraction call → parse RawJob list | `config`, `defaults`, `models.schemas`, `scraping.json_utils`, `scraping.site_prompts`, `llm.gemini_client` | `scraping.orchestrator`, `api.queue` |
| `backend/scraping/adaptive_scraper.py` | AdaptiveScraper (Tier 2): browser-use Agent loop (max 20 steps, 180 s timeout, 2 retries); scrape_job_listings() and scrape_job_details(); graceful empty-list fallback | `config`, `defaults`, `models.schemas`, `scraping.json_utils`, `scraping.site_prompts`, `utils.browser_path` | `scraping.orchestrator` |
| `backend/scraping/adzuna_client.py` | AdzunaClient: httpx GET to Adzuna v1 REST API; JobFilters → query params; maps JSON → RawJob list; AdzunaAPIError | `config`, `matching.filters`, `models.schemas` | `scraping.orchestrator`, `api.jobs` |
| `backend/scraping/session_manager.py` | BrowserSessionManager: get_or_create_session() (auto-login for linkedin/indeed using stored credentials; manual login WS flow for others; 10-min timeout); confirm_login()/cancel_login() WS signal receivers; list_sessions()/clear_session() | `config`, `models.user`, `database`, `scraping.site_prompts`, `api.ws`, `api.ws_models`, `utils.browser_path` | `scraping.orchestrator`, `api.settings`, `main` |
| `backend/scraping/deduplicator.py` | JobDeduplicator: MD5 hash of normalized (company, title, location); deduplicate() keeps longer description on collision | `models.schemas` | `scraping.orchestrator`, `api.jobs` |
| `backend/scraping/site_prompts.py` | SITE_PROMPTS dict (browser-use agent prompts per site), SITE_CONFIGS dict (metadata/type/login flags per site), SITE_CONTENT_SELECTORS dict (Tier 1 CSS scoping), EXTRACTION_PROMPTS dict (Tier 1 parse-only prompts), format_prompt() | — | `scraping.orchestrator`, `scraping.scrapling_fetcher`, `scraping.adaptive_scraper`, `scraping.session_manager`, `api.settings`, `database` |
| `backend/scraping/json_utils.py` | extract_json_from_text(): finds first JSON array or object in LLM free-text; parse_jobs_from_json(): maps raw dicts to RawJob list | `models.schemas` | `scraping.scrapling_fetcher`, `scraping.adaptive_scraper` |
| `backend/security/__init__.py` | Package marker (empty stub) | — | Python import system |
| `backend/security/sanitizer.py` | InputSanitizer: sanitize_for_prompt() (truncate + strip control chars + collapse whitespace + strip injection patterns); wrap_untrusted(); sanitize_url() | `defaults` | `llm.job_analyzer`, `llm.cv_modifier`, `llm.cv_editor` |
| `backend/utils/__init__.py` | Package marker (empty stub) | — | Python import system |
| `backend/utils/retry.py` | async_retry() decorator: exponential-backoff retry for flaky async functions | — (stdlib only) | (utility; used ad-hoc) |
| `backend/utils/source_health.py` | SourceHealthMonitor: in-memory per-source success/failure rate tracker; SourceHealthRecord dataclass; module-level health_monitor singleton | — (stdlib only) | (utility; used ad-hoc) |
| `backend/utils/browser_path.py` | get_chromium_executable(): resolves Chromium binary via patchright then playwright subprocess probe; cached after first call | — (stdlib only) | `scraping.adaptive_scraper`, `scraping.session_manager`, `applier.form_filler` |
